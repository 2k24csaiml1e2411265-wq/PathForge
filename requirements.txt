# PathForge — Python dependencies
# Tested with Python 3.10 - 3.12

# --- Core / data ---
pandas>=2.0.0
numpy>=1.24.0

# --- Web app ---
streamlit>=1.32.0

# --- Document processing ---
pypdf>=4.0.0

# --- NLP / skill extraction ---
spacy>=3.7.0
# After installing requirements, download the small English model:
#   python -m spacy download en_core_web_sm

# --- ML / embeddings / similarity ---
scikit-learn>=1.3.0
sentence-transformers>=2.5.0   # optional — see README "Offline mode". Falls back to TF-IDF if unavailable.
faiss-cpu>=1.7.4                # falls back to a NumPy/scikit-learn nearest-neighbour search if unavailable

# --- Visualization (Streamlit dashboard) ---
plotly>=5.18.0

# --- Gemini LLM integration ---
google-genai>=0.3.0            # optional — app runs in fallback mode without an API key

# --- Config / env ---
python-dotenv>=1.0.0

# --- Testing ---
pytest>=7.4.0
